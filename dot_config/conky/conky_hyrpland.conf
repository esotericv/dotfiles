-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'top_right',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'CaskaydiaCove Nerd Font:size=11',
    gap_x = 10,
    gap_y = 10,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = true,
    out_to_x = false,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    own_window_argb_visual = true,
    own_window_argb_value = 100,
    own_window_colour = '00000B', #'111216',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
}

-- Variables: https://conky.cc/variables
conky.text = [[
#${color grey}Info:$color ${scroll 32 $sysname $nodename $kernel}
$color ${alignc}Arch $sysname $kernel
$hr
${color white}CPU:
#${color grey}Frequency (MHz):$color $freq
#${color grey}Frequency (GHz):$color $freq_g
${color grey}Frequency:$color $freq
${color grey}Usage:$color $cpu% ${cpubar 4}
# this method of fetching cpu temp is iffy, better version below ${color grey}Temp:$color ${hwmon 1 temp 1}°C
${color grey}Temp:$color ${execi 5 sensors | grep Tctl | cut -c16-17}°C
#$color $acpitemp °C # seemingly inaccurate temps
$hr
${color white}GPU:
${color grey}Usage: $color${execi 5 cat /sys/class/drm/card1/device/gpu_busy_percent}% 
${color grey}Temp: $color${execi 5 sensors | grep "edge" | cut -c16-17}°C
$hr
${color}Memory:
${color grey}RAM:$color $mem/$memmax - $memperc%
#${membar 4}
${color grey}Swap:$color $swap/$swapmax - $swapperc% 
#${swapbar 4}
$hr
${color white}File Systems:
 ${color grey}/ $color${fs_used /}/${fs_size /} - ${fs_used_perc}%
 #${fs_bar 6 /}
${color grey}g $color${fs_used /mnt/games}/${fs_size /mnt/games} - ${fs_used_perc /mnt/games}%
 #${fs_bar 6 /mnt/games}
${color grey}s $color${fs_used /mnt/ssd}/${fs_size /mnt/ssd} - ${fs_used_perc /mnt/ssd}%
]]
